diff --git a/drivers/net/wireless/ath/wcn36xx/hal.h b/drivers/net/wireless/ath/wcn36xx/hal.h
index ef9ea4ff8..c8b50c8e2 100644
--- a/drivers/net/wireless/ath/wcn36xx/hal.h
+++ b/drivers/net/wireless/ath/wcn36xx/hal.h
@@ -2174,6 +2174,9 @@ struct wcn36xx_hal_config_bss_params_v1 {
 	 *  On STA - Add the AP to which this STA is joining in an
 	 *  "STA context"
 	 */
+	u32 dot11_mode;
+	u32 phy_mode;
+
 	struct wcn36xx_hal_config_sta_params_v1 sta;
 
 	u8 vht_capable;
@@ -4166,6 +4169,9 @@ struct wcn36xx_hal_tx_compl_ind_msg {
 
 	/* Tx Complete Indication Success or Failure */
 	u32 status;
+
+	/* Dialog token (present in WCN3680 Pronto FW; matches prima) */
+	u32 dialog_token;
 };
 
 struct wcn36xx_hal_wlan_host_suspend_ind_msg {
diff --git a/drivers/net/wireless/ath/wcn36xx/smd.c b/drivers/net/wireless/ath/wcn36xx/smd.c
index 813553edc..aba5e6e0e 100644
--- a/drivers/net/wireless/ath/wcn36xx/smd.c
+++ b/drivers/net/wireless/ath/wcn36xx/smd.c
@@ -1706,13 +1706,18 @@ static int wcn36xx_smd_config_bss_v1(struct wcn36xx *wcn,
 
 	memcpy(&bss->ext_set_sta_key_param,
 	       &bss_v0.ext_set_sta_key_param,
-	       sizeof(bss_v0.acvo));
+	       sizeof(bss_v0.ext_set_sta_key_param));
 
 	bss->wcn36xx_hal_persona = bss_v0.wcn36xx_hal_persona;
 	bss->spectrum_mgt_enable = bss_v0.spectrum_mgt_enable;
 	bss->tx_mgmt_power = bss_v0.tx_mgmt_power;
 	bss->max_tx_power = bss_v0.max_tx_power;
 
+	/* WCN3680 Pronto 1.5.1.2 expects these fields before sta context.
+	 * Keep them as 0 to see if the offset was the only issue. */
+	bss->dot11_mode = 0;
+	bss->phy_mode = 0;
+
 	wcn36xx_smd_set_bss_vht_params(vif, sta_80211, bss);
 
 	PREPARE_HAL_BUF(wcn->hal_buf, (*msg_body));
