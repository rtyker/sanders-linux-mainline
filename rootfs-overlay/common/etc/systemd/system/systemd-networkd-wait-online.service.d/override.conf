[Service]
ExecStart=
# --any: so precisa de UMA interface pra prosseguir (evita trava de
# 2 min esperando wlan0/usb0/enx* que podem nao existir).
# Sem -i: detecta qualquer interface disponivel no boot.
ExecStart=/usr/lib/systemd/systemd-networkd-wait-online --any
